// Cron route for deadline reminders
export async function GET() {
  // Placeholder — would query Supabase for upcoming deadlines and send via Resend
  return Response.json({ message: 'Reminders processed' })
}
